<div class="flex items-center p-4 bg-base-300 rounded-lg shadow-sm">
  <%= form_with model: quest_item, local: true, class: 'inline space-x-3' do |form| %>
    <%= form.check_box :completed, class: "checkbox" %>
    <span><%= quest_item.name %></span>
  <% end %>
</div>